#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(0);
    cout.tie(0);
    cout.sync_with_stdio(0);
    cin.sync_with_stdio(0);
    int t;
    string input;
    cin >> t;
    while (t--)
    {
        cin >> input;
        string ans = tolower(input);
    }
}